interface:
  display_name: "Kimi Swarm Prompt"
  short_description: "Codex-only Kimi swarm prompt flow"
  default_prompt: "Use $kimi-agent-swarm-prompt in Codex to refine my request into a prompt contract and route it to a Kimi swarm-style workflow."
policy:
  allow_implicit_invocation: true
